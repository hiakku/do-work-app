import { createTheme } from "@mui/material/styles";

let theme = createTheme({
  palette: {
    primary: {
      main: "#146ca4",
      normal: "#FFF",
    },
    secondary: {
      main: "#1F2A3C",
    },
  },
});

export default theme;
